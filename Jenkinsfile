pipeline {
    agent any

    stages {
        stage('Pulling Repository')
      {
        steps{
       git branch: 'main', url: 'https://github.com/AdwaithS22/CICD1.git'
        }
        }
    }
}
